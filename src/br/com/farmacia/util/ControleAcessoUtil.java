/*package br.com.farmacia.util;

import java.io.IOException;

import javax.servlet.Filter;
import javax.servlet.FilterChain;
import javax.servlet.FilterConfig;
import javax.servlet.ServletException;
import javax.servlet.ServletRequest;
import javax.servlet.ServletResponse;
import javax.servlet.annotation.WebFilter;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpSession;

@WebFilter(servletNames = { "Faces Servlet" })
public class ControleAcessoUtil implements Filter {

	@Override
	public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain)
			throws IOException, ServletException {
		// TODO Auto-generated method stub
		//HttpServletRequest req = (HttpServletRequest) request;
		//HttpSession session = req.getSession();
		//if (session!= null) {
		//	if ((session.getAttribute("Logado") != null) 
		//			|| req.getRequestURI().endsWith("faces/pages/fornecedores.xhtml")
		//			|| req.getRequestURI().endsWith("funcionario.xhtml") || req.getRequestURI().endsWith("index.xhtml")
		//			|| req.getRequestURI().endsWith("javax.faces.resource/")
		//			|| !req.getRequestURI().endsWith("login.xhtml")) {
		//		System.out.println("danadinho");
		//	}
		//}
	}

	@Override
	public void destroy() {
		// TODO Auto-generated method stub

	}

	@Override
	public void init(FilterConfig filterConfig) throws ServletException {
		// TODO Auto-generated method stub

	}

}
*/